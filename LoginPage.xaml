<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="productoApp.LoginPage"
             Title="Login Page">
    <VerticalStackLayout>
        <StackLayout>
            <Entry x:Name="Username" Placeholder="Nombre de Usuario"/>
            <Entry x:Name="Password" Placeholder="Contraseña" IsPassword="True"/>
        </StackLayout>

        <Button Text="Ingresar" Clicked="OnClickLogin"/>
    </VerticalStackLayout>
</ContentPage>